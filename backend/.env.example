PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=ecommerce_super_secret_key_change_in_production_2024
JWT_EXPIRES_IN=7d

# Prisma will use SQLite database (no password needed!)
# Database file will be created at: backend/prisma/dev.db
